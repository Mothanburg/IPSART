#include "GaRS.h"

int main() {
  // constexpr long H = 25;
  // constexpr long W = 25;
  // constexpr unsigned long long LEN = static_cast<unsigned long long>(H * W);

  // CloudePottier(H, W, new double[LEN], new double[LEN], new double[LEN], new
  // double[LEN], new double[LEN], new double[LEN], new double[LEN], new
  // double[LEN], new double[LEN], new double[LEN], new double[LEN], new
  // double[LEN]);

  RefinedLeeFilter(0, 0, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr);

  return 0;
}